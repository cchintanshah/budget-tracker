rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Secure the database to only allow users who have logged in via the app
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
